<h2 class="m-0 p-3 text-center"><%= @list.name %></h1>
<div class="flex-grow-1 px-3 overflow-y-auto overflow-x-hidden" style="scrollbar-width: none;">
  <div class="row g-3">
    <% @list.movies.each do |movie| %>
      <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
      <div class="col-6 col-sm-3">
        <div class="card h-100">
          <div class="poster-wrapper">
            <img src="<%= movie.poster_url %>" class="poster-image" alt="<%= movie.title %>">
          </div>
          <div class="p-3 h-100">
            <small class="">Rating: <%= movie.rating %></small><br>
            <small class="m-0 fw-bold">
              <%= movie.title %>
            </small>
          </div>
          <% if @list.user == current_user %>
            <%= link_to "Delete", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-delete w-100" %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
<div class="flex-shrink-0 border-top d-flex flex-column gap-3 p-3">
  <% if @list.user == current_user %>
    <%= link_to "Add movie", new_list_bookmark_path(@list), class: "btn btn-primary" %>
  <% end %>
  <%= link_to "Back", lists_path, class: "btn btn-primary" %>
</div>
